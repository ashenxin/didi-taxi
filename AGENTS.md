# AGENTS.md

本文档维护后端技术栈、模块地图、修改授权和开发协作方式。项目介绍与一期范围见 `README.md`，二期功能范围与状态见 `二期功能/README.md`。

## 后端项目

- 本项目为 Java 21 + Spring Boot 3 后端应用，采用 Maven 多模块结构。
- 构建工具：Maven。
- 微服务框架：Spring Cloud 2025、Spring Cloud Alibaba 2025。
- 配置中心与服务发现：Nacos。
- 网关与服务调用：Spring Cloud Gateway、OpenFeign、Spring Cloud LoadBalancer。
- 数据访问：MyBatis-Plus 3 + MySQL。
- 缓存与消息：Redis、Kafka。
- 实时通信：Spring WebSocket。
- 身份认证：Spring Security、JWT。
- 任务调度：XXL-JOB 3。
- AI 能力：Spring AI 1、Spring AI Alibaba、DashScope。
- 测试与覆盖率：Spring Boot Test、JaCoCo。

## 模块地图

| 模块 | 默认端口 | 主要职责 | 主要阶段 |
|---|---:|---|---|
| `gateway` | 18080 | 三端路由、JWT 验签、CORS、可信身份头注入和 WebSocket 握手放行 | 共用 |
| `admin-api` | 8099 | 管理后台 BFF | 共用 |
| `passenger-api` | 8100 | 乘客端 BFF | 共用 |
| `driver-api` | 8101 | 司机端 BFF | 共用 |
| `capacity` | 8090 | 司机、车辆、公司/车队、听单、GEO 和派单匹配 | 一期，二期扩展换队 |
| `calculate` | 8091 | 基础计价 | 一期，二期扩展优惠券、签到和积分 |
| `passenger` | 8092 | 乘客账号及后台权限相关内部能力 | 一期，二期扩展账号生命周期 |
| `order` | 8093 | 订单状态机、事件、派单协作和超时治理 | 一期，二期扩展结算与司机行程 |
| `map` | 8094 | 高德路线、地理编码和逆地理编码 | 一期，二期扩展 AI 路线规划 |
| `wallet` | 8095 | 免密协议和支付单 | 二期 |
| `xxl-job-admin` | 8081 | XXL-JOB 调度中心 | 共用 |

## 修改授权约定

- 探索代码、读取配置、查询日志以及其他只读排查可以直接进行。
- 新增、编辑或删除代码、配置、SQL、测试及文档前，必须先说明拟修改范围并获得用户明确确认；分析或排查请求本身不视为修改授权。
- 用户确认后，只能在当次确认的范围内写入，不得扩展到尚未讨论的功能。

## 开发约定

- 后端服务日常由用户通过 IDEA 运行配置启动和自动重启。除非用户明确要求，不得自行启动、停止或重启任何后端进程。
- 先根据代码、配置、数据库、日志、测试和既有设计核验事实；当用户判断与证据、业务边界、安全要求或一致性约束冲突时，应明确说明风险和正确做法。
- 修改前检查工作区状态，保留用户已有改动，不覆盖、不回退、不顺带整理无关文件。
- 修改功能前先阅读对应 PRD、TECH、API、SQL 和 TEST 文档；若文档与实现冲突，应先指出冲突并确认权威口径。
- 修改接口、数据库结构或关键业务规则时，同步维护相应文档和测试，避免实现与文档分叉。
- 涉及并发、幂等、鉴权、金额、状态推进或数据迁移时，必须补充与风险相匹配的验证，不以编译通过代替业务验收。
- 测试、构建和格式检查应限定在本次修改涉及的模块；不得因验证而操作未获授权的服务进程或外部数据。
- Git 提交前只纳入本次授权范围内的文件。
- Git 提交信息格式统一为 `type(scope): description`。
  - `type` 只使用：`feat`、`fix`、`docs`、`style`、`refactor`、`test`、`chore`。
  - `scope` 使用本次变更涉及的模块或功能名称。
  - `description` 使用简洁中文；模块名、代码标识和通用缩写可以保留英文。
- 文档名大量使用中文，检索文件和内容时优先使用 `rg`，不要只依赖 IDE 侧边栏。
