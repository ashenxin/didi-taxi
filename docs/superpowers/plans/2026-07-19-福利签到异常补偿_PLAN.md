# 福利签到异常补偿实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 calculate 服务增加福利签到定时对账，自动收敛 Redis Bitmap，并将 MySQL 账本异常只记录到 `benefit_reconciliation_issue`，不自动修改积分。

**Architecture:** MySQL 签到记录始终是 Bitmap 权威来源；XXL-Job Handler 解析限定范围参数并调用独立对账服务。对账服务按用户分页扫描、幂等写入或恢复异常记录；XXL-Job 自身日志承担批次记录，因此不创建 reconciliation run 表。

**Tech Stack:** Java 21、Spring Boot 3.3.5、MyBatis-Plus、Redis、XXL-JOB 3.1.0、JUnit 5、H2。

## Global Constraints

- 只新增 `benefit_reconciliation_issue` 一张表。
- Redis Bitmap 允许自动补齐或重建；签到记录、积分流水、积分账户只检测，不自动修改。
- 不新增任何 HTTP 手动触发接口；人工执行只通过 XXL-Job 控制台。
- 换号后 `customerId` 不变，不迁移积分；注销漏通知当前无法仅凭 calculate 本地数据识别，必须在文档明确边界。
- Git 提交信息使用中文；完成代码、SQL、文档与验证后生成独立提交。

---

### Task 1: 对账参数和异常持久化

**Files:**
- Create: `calculate/src/main/java/com/sx/calculate/model/BenefitReconciliationIssue.java`
- Create: `calculate/src/main/java/com/sx/calculate/dao/BenefitReconciliationIssueMapper.java`
- Create: `calculate/src/main/java/com/sx/calculate/service/BenefitReconciliationService.java`
- Test: `calculate/src/test/java/com/sx/calculate/service/BenefitReconciliationServiceTest.java`

**Interfaces:**
- Consumes: `benefit_sign_record`、`benefit_points_flow`、`benefit_points_account` 与 Redis Bitmap。
- Produces: `BenefitReconciliationService.reconcile(String jobParam)`，返回任务摘要并维护 OPEN/RESOLVED 异常。

- [x] **Step 1: 写参数校验、Bitmap 补齐、MySQL 异常留痕与恢复的失败测试**
- [x] **Step 2: 运行定向测试，确认因实现缺失而失败**
- [x] **Step 3: 实现最小的分页扫描、Redis 收敛和异常 upsert/resolve**
- [x] **Step 4: 运行定向测试并确认通过**

### Task 2: XXL-Job 接入

**Files:**
- Modify: `calculate/pom.xml`
- Create: `calculate/src/main/java/com/sx/calculate/config/XxlJobConfig.java`
- Create: `calculate/src/main/java/com/sx/calculate/job/BenefitReconciliationJob.java`
- Modify: `calculate/src/main/resources/application.yml`
- Test: `calculate/src/test/java/com/sx/calculate/job/BenefitReconciliationJobTest.java`

**Interfaces:**
- Consumes: XXL-Job `benefitSignReconciliation` 的 JSON 参数。
- Produces: 调用 `BenefitReconciliationService.reconcile` 并向任务日志输出摘要。

- [x] **Step 1: 写 Handler 委托测试并确认失败**
- [x] **Step 2: 增加 xxl-job-core、条件化执行器配置和 Handler**
- [x] **Step 3: 运行 Handler 测试并确认通过**

### Task 3: SQL 与文档同步

**Files:**
- Modify: `calculate/src/main/resources/sql/calculate_schema.sql`
- Modify: `calculate/src/test/resources/schema-test.sql`
- Modify: `二期功能/乘客端_福利签到_SQL.md`
- Modify: `二期功能/乘客端_福利签到_异常补偿_TECH.md`
- Modify: `二期功能/乘客端_福利签到_TECH.md`
- Modify: `二期功能/乘客端_福利签到_PRD.md`
- Modify: `二期功能/README.md`

**Interfaces:**
- Produces: 与代码字段一致的单表 DDL，以及换号/销号、无 run 表、XXL 日志承担批次记录的准确说明。

- [x] **Step 1: 在生产 schema 与 H2 测试 schema 增加 issue 表**
- [x] **Step 2: 删除 run 表设计并更新任务流程、测试和边界说明**
- [x] **Step 3: 执行 `mvn -pl calculate test`、`git diff --check` 与关键字一致性扫描**

### Task 4: 完成前核对与提交

- [x] **Step 1: 补充历史月 Bitmap 重建成功及 `PERSIST` 失败回归**
- [x] **Step 2: 同步 README、AGENTS、TODO、API/PRD/TECH/SQL/TEST 文档口径**
- [x] **Step 3: 执行 `mvn -pl calculate verify`，34 个测试通过，JaCoCo 门禁通过**
- [x] **Step 4: 执行 `git diff --check` 和旧口径关键字扫描**
