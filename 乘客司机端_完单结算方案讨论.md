# 乘客司机端：完单结算方案讨论

> 文档状态：历史讨论稿；核心决策已确认并已落地为 MVP 本地模拟闭环。本文保留原讨论过程便于追溯；与下列权威文档冲突时，以权威文档为准。
> 权威文档：[方案设计](docs/superpowers/specs/2026-07-17-完单结算方案设计.md)、[接口说明](docs/api/完单结算接口说明.md)、[本地模拟联调手册](docs/testing/完单结算本地模拟联调手册.md)、[实施计划](docs/superpowers/plans/2026-07-17-完单结算实施计划.md)。
> 历史术语说明：下文原有的 `PROCESSING` 对应当前 `CALCULATING`，`PAYMENT_FAILED` 对应当前 `PAYMENT_REQUIRED`；原文未确认的方案已由“已确认决策与落地摘要”覆盖。

## 已确认决策与落地摘要（2026-07-17）

- 司机完单只表示行程结束；`order-service` 可靠登记并异步编排结算。已实现结算查询 `GET /app/api/v1/orders/{orderNo}/settlement` 和主动支付 `POST /app/api/v1/orders/{orderNo}/payments`。
- 距离、预计时长、实际计费时长均是稳定的本地 mock 数据；下单冻结路线与计价规则，首次结算冻结实际时长。客户端不能提交或覆盖结算距离、时长和金额。
- 费用先按冻结规则计算，再减优惠后支付。金额先按 `HALF_UP` 舍入到两位并校验：`0.01 ≤ finalAmount ≤ 10000.00`，`0 ≤ discountAmount ≤ finalAmount`；合法优惠使应付恰为 `0.00` 的零元单单独结清，其他非法金额不得静默归零。
- 免密失败、无免密协议、主动支付失败或取消均进入 `PAYMENT_REQUIRED`；乘客以新的 `Idempotency-Key` 主动选择 `ALIPAY` 或 `WECHAT`。主动支付请求体只接受 `channel`，不接受金额、乘客或优惠券字段。
- 支付明确失败后没有后台定时自动重试；优惠券保持 `LOCKED`，由乘客主动支付。长期异常提示联系运营。结果未知仅查询原交易，处于 `PAY_CONFIRMING` 时不创建第二笔支付。
- 存在未结清订单时禁止下新单；成功结清后才解除限制。真实支付宝/微信金融渠道、退款和对账未实现；司机金额展示及车队/运营公司固定金额或比例分成也未实现。

---

## 1. 为什么需要单独讨论（历史背景）

讨论开始时，项目具备以下分散能力：

- 司机可以调用完单接口，把订单从 `STARTED` 推进到 `FINISHED`。
- `calculate-service` 可以查询可用券、锁券、核销和释放优惠券。
- `wallet-service` 可以根据免密协议创建支付单并执行 mock 自动扣款。
- `order-service` 已有 `trip_order_settlement`，可以保存本单最终金额、优惠和支付快照。
- 乘客端已具备 `ORDER_CHANGED` WebSocket 通知及收到通知后重新查询订单详情的机制。

当时这些能力尚未在“司机完单”之后自动串起来，主要风险是：

1. 完单、计价、用券、扣款是不同服务内的操作，不能依靠一个本地数据库事务保证全部同时成功。
2. 司机端提交的里程、时长和 `finalAmount` 目前会进入完单请求，不应直接作为真实扣款依据。
3. 接口如果把“行程结束”和“支付成功”混成一个结果，会在异步失败时对用户产生错误承诺。
4. 支付成功后仍可能发生核券、写结算快照或通知失败，需要明确补偿方式。
5. 前端需要区分“结算中、已支付、支付失败”，也需要知道是否使用了优惠券，不能只看到订单 `FINISHED`。

因此需要先把产品状态、金额口径、服务职责和失败补偿讨论清楚，再进入实现。

---

## 2. 原始代码现状（已由当前 MVP 实现替代）

### 2.1 当前入口

司机端对外完单入口：

```http
POST /driver/api/v1/orders/{orderNo}/finish
```

当前调用关系：

```text
司机端
  -> driver-api
  -> order-service 完单
  -> driver-api 通知 passenger-api
  -> passenger-api 推送 ORDER_CHANGED
```

### 2.2 当前完单行为

`order-service` 当前主要完成：

- 校验当前司机和订单状态。
- 使用 CAS 将订单从 `STARTED` 推进到 `FINISHED`。
- 写入 `finishedAt`、`finalAmount` 和 `ORDER_FINISHED` 事件。
- 如果请求没有传 `finalAmount`，当前会回退使用 `estimatedAmount`。

当前还没有自动完成：

- 服务端最终计价。
- 自动选择并锁定最优优惠券。
- 创建订单结算快照。
- 调用钱包免密扣款。
- 支付成功后的优惠券核销。
- 支付失败后优惠券保持锁定、乘客主动支付及结算状态查询。
- 面向乘客的完整结算状态查询。

### 2.3 当前接口的金额信任问题

当前完单请求包含：

```json
{
  "distanceKm": 12.3,
  "durationMin": 28,
  "finalAmount": 35.60
}
```

这些值由司机端提交。正式结算时，司机端只能表达“请求结束行程”，不能决定乘客应付金额。否则客户端篡改、旧版本计算差异或请求重放都可能影响真实扣款。

**已确认并落地：司机完单请求只负责结束行程；结算使用的里程、时长、计价规则和金额全部由服务端取得或计算。**

---

## 3. 已统一的概念

“完单成功”至少包含三个不同层次，不能用一个状态表达：

| 层次 | 建议字段 | 典型状态 | 含义 |
| --- | --- | --- | --- |
| 行程状态 | `orderStatus` | `STARTED / FINISHED` | 司机是否已经结束本次行程 |
| 结算状态 | `settlementStatus` | `CALCULATING / PAY_CONFIRMING / PAYMENT_REQUIRED / PAID` | 计价、用券、支付和结算是否整体完成 |
| 支付状态 | `paymentStatus` | `INIT / PAYING / SUCCESS / FAILED` | 钱包支付单本身的结果 |

必要时还可以独立记录：

| 字段 | 典型状态 | 用途 |
| --- | --- | --- |
| `couponStatus` | `NOT_APPLICABLE / LOCKED / USED / FINALIZING` | 判断优惠券目前处于哪个阶段；支付失败、取消和待确认期间保持 `LOCKED` |
| `settlementStage` | `FARE_CALCULATING / COUPON_LOCKING / PAYING / COUPON_FINALIZING` | 运维排障与前端结算中提示 |

核心口径：

- `orderStatus=FINISHED` 只表示行程已结束，不等于已经支付。
- `paymentStatus=SUCCESS` 只表示钱已扣成功；如果核券和结算快照尚未完成，整体结算仍不应标记为 `PAID`。
- `settlementStatus=PAID` 才表示本单计价、优惠、实付和结算结果已经全部固化。

---

## 4. 异步结算为何不能承诺支付成功（已确认）

### 4.1 已确认结论

会有这种风险，但根因不是异步本身，而是接口返回了超出当前事务事实的结果。

司机完单接口不应返回“支付成功”，只应返回：

```text
行程已结束，费用结算中
```

建议响应示例：

```json
{
  "code": 200,
  "msg": "行程已结束，费用结算中",
  "data": {
    "orderNo": "T202607170001",
    "orderStatus": "FINISHED",
    "settlementStatus": "CALCULATING"
  }
}
```

它只承诺两件已经落库的事实：

1. 行程状态已经成功推进到 `FINISHED`。
2. 结算任务已经可靠登记，之后可以重试和恢复。

### 4.2 推荐触发方式

推荐在 `order-service` 完单事务内同时写入结算 Outbox 事件：

```text
同一个本地事务
  1. STARTED -> FINISHED
  2. 写 ORDER_FINISHED 事件流水
  3. 写 ORDER_SETTLEMENT_REQUESTED Outbox
事务提交
```

之后由 Outbox 投递器把事件发到 Kafka，结算消费者异步执行计价、用券和支付。

这样可以避免以下断点：

- 订单已完单，但进程在发送结算消息前宕机。
- BFF 调用完单成功，但后续同步调用某个服务超时，无法判断是否需要重新完单。
- BFF 重试导致状态冲突或重复发起支付。

### 4.3 为什么不建议由 `driver-api` 顺序编排所有步骤

`driver-api` 是司机端 BFF，适合端侧协议转换与读取聚合，不适合作为持久化的结算工作流引擎。若由它依次调用：

```text
完单 -> 计价 -> 锁券 -> 扣款 -> 核券 -> 写结算
```

任何一步超时都会让 BFF 无法区分“下游没有执行”和“已经执行但响应丢失”，且 BFF 重启后也没有可靠任务继续推进。

### 4.4 支付成功后，后续异步步骤失败怎么办

不能把整个流程简单回滚，因为跨服务不能共享数据库事务，而且真实支付成功后也不能通过数据库回滚把钱自动还给乘客。

建议按以下规则处理：

| 失败点 | 处理方式 | 是否再次扣款 |
| --- | --- | --- |
| 最终计价失败 | 保持 `CALCULATING`，恢复计价任务；长期异常联系运营 | 否 |
| 锁券失败 | 保持 `CALCULATING`，恢复或运营处理；不静默降级为无券支付 | 否 |
| 支付请求超时 | 按支付幂等键查询原支付单结果，再决定是否重试调用 | 不创建第二笔支付单 |
| 支付明确失败 | 保持优惠券 `LOCKED`，进入 `PAYMENT_REQUIRED`，由乘客主动支付 | 不做后台定时自动重试 |
| 支付成功、核券失败 | 只重试核券和结算收尾 | **绝不再次扣款** |
| 支付成功、通知失败 | 重试通知；HTTP 查询仍可读到权威结算状态 | 否 |

### 4.5 幂等键建议

各步骤都必须能重复执行：

```text
结算任务：trip-settlement:{orderNo}
支付单：trip-payment:{orderNo}
锁券/核券：orderNo（calculate-service 已围绕订单号保存锁定关系）
通知：orderNo + settlementVersion 或 eventSeq
```

关键约束：

- 一个订单只能有一份当前有效结算记录。
- 一个订单只能对应一笔有效自动支付业务单；重试读取或推进原支付单，不能新建另一笔。
- 消费 Kafka 消息前后都要按业务状态判断，不能仅依赖 Kafka “只消费一次”。
- `settlementStatus=PAID` 后再次消费结算事件应直接成功返回。

### 4.6 支付成功的判定

只有同时满足以下条件，才能向乘客展示“支付成功”：

1. 钱包支付结果已持久化为 `SUCCESS`。
2. `paidAmount` 与本次结算 `payableAmount` 一致。
3. 有券时优惠券已经核销；无券时已经明确记录无券原因。
4. `trip_order_settlement` 已保存最终金额快照并标记 `PAID`。

对于“实付 0 元”的订单，不调用钱包扣款；优惠券核销和结算快照完成后直接进入 `PAID`，并记录支付方式为 `ZERO_AMOUNT` 或等价值。

### 4.7 本项已确认

- [x] 司机完单成功只代表行程结束，接口返回结算中。
- [x] `order-service` 以本地事务 + Outbox 可靠触发结算。
- [x] 支付成功但核券失败时，只重试核券和结算收尾，绝不再次扣款。
- [x] 自动支付明确失败后由乘客主动支付；没有后台定时自动重试。
- [x] 锁券系统错误保持 `CALCULATING` 等待恢复或运营处理，不静默降级为无券支付。

---

## 5. `finalAmount` 的计算口径（已确认）

### 5.1 已确认结论

`finalAmount` 必须由后端计算，不能信任司机端传值，也不能由前端计算后提交。

建议把金额拆清楚：

```text
finalAmount       = 行程结束后的优惠前最终车费
discountAmount    = 本单优惠金额
payableAmount     = finalAmount - discountAmount
paidAmount        = 实际支付成功金额
platformFee       = payableAmount * 5%（当前已讨论口径）
carrierIncome     = payableAmount - platformFee
```

其中 `finalAmount` 不应被用来同时表达“优惠前车费”和“乘客实付”，否则后续退款、发票和对账无法解释。

### 5.2 高德距离能不能直接作为测距标准

高德路径规划接口可以返回规划路线的距离与预计时长，适合：

- 下单前估价。
- MVP 阶段没有完整轨迹时，作为双方事先可解释的约定路线距离。
- 路线展示和预计到达时间。

但它不天然等于车辆实际行驶距离：

- 路径规划结果是“推荐路线”，不是车辆真实轨迹。
- 实际可能绕行、临时改道、乘客要求中途变更目的地。
- 道路数据、交通策略、API 参数和算法升级都可能使同一组起终点在不同时刻返回不同结果。
- 如果完单时重新请求路径规划，结果可能和下单时不同，历史账单难以复现。

因此建议分阶段处理。

#### MVP 已落地口径

```text
计费距离 = 下单时服务端冻结的本地 mock 规划距离
预计时长 = 下单时服务端冻结的本地 mock 预计时长
计费时长 = 首次结算时服务端生成并冻结的本地 mock 实际计费时长
计价规则 = 下单时冻结的 fareRuleSnapshot
```

这些 mock 指标相同输入和版本稳定可复现，且司机端不能篡改金额。`startedAt`、`finishedAt` 仅用于行程审计，不参与本期费用计算。

**完单时不要再次调用高德路径规划重新算距离。** 应使用下单时已经保存的路线结果和规则快照，保证同一订单在重试结算时结果不漂移。

#### 正式计费建议口径

```text
司机端周期上报 GPS 点
  -> 服务端校验、去噪、纠偏/绑路
  -> 形成订单实际轨迹
  -> 计算有效行驶距离
  -> 结合服务端 startedAt / finishedAt 计算实际时长
  -> 使用冻结计价规则计算最终车费
```

高德的轨迹纠偏/绑路能力可以作为轨迹处理工具，但计费标准仍应是平台明确的业务规则、采样质量控制、异常轨迹处理和证据留存，不能简单表述为“高德返回多少就收多少”。

高德官方能力说明可参考：

- [驾车路径规划 API](https://lbs.amap.com/api/webservice/guide/api/direction)
- [轨迹纠偏 API](https://lbs.amap.com/api/webservice/guide/api/grasproad)

### 5.3 MVP 最终车费计算建议

建议在 `calculate-service` 新增内部最终计价能力，例如：

```http
POST /internal/calculate/final-fare
```

概念请求：

```json
{
  "orderNo": "T202607170001",
  "companyId": 1001,
  "cityCode": "330100",
  "productCode": "EXPRESS",
  "distanceMeters": 12340,
  "durationSeconds": 1680,
  "fareRuleSnapshot": "{...}",
  "distanceSource": "ORDER_PLANNED_ROUTE"
}
```

概念响应：

```json
{
  "finalAmount": 35.60,
  "distanceAmount": 18.20,
  "durationAmount": 7.40,
  "otherAmount": 0.00,
  "currency": "CNY",
  "calculationVersion": "fare-v1"
}
```

具体公式继续复用计价规则，不应在 `order-service`、`driver-api` 和前端各复制一套。

### 5.4 下单时需要冻结的数据

为了保证完单可复算，建议订单创建成功时至少保存：

| 字段 | 含义 |
| --- | --- |
| `plannedDistanceMeters` | 下单时采用的规划距离 |
| `plannedDurationSeconds` | 下单时采用的规划时长，主要用于估价展示和审计 |
| `routeStrategy` | 路径规划策略/产品参数 |
| `distanceSource` | `LOCAL_MOCK_ROUTE` |
| `fareRuleSnapshot` | 本单采用的完整计价规则快照 |
| `fareCalculationVersion` | 计价算法版本 |
| `estimatedAmount` | 下单时预估车费 |

当前模型已有 `fareRuleSnapshot` 字段，但需要确认下单主链是否真的写入了完整快照；若仍为 `null`，实现结算前必须补齐。

### 5.5 金额精度建议

- 内部金额统一使用 `BigDecimal`，数据库使用固定精度 `DECIMAL`。
- 每一个计价分项先保留足够精度，最后按统一规则舍入到分。
- 明确使用 `HALF_UP` 或财务确认的舍入规则，不能依赖默认行为。
- 所有结算重试必须使用同一份输入快照和算法版本。
- 结算表保存分项和输入摘要，避免只保存一个无法解释的总金额。

### 5.6 本项已确认

- [x] MVP 以稳定本地 mock 规划距离、预计时长和服务端冻结的 mock 实际计费时长作为计价输入。
- [x] MVP 暂不建设完整 GPS 轨迹计费，也不调用外部地图 API。
- [x] 完单时不重新规划路线，重复结算只读取原快照。
- [x] 司机完单请求的 `distanceKm`、`durationMin`、`finalAmount` 不参与结算。
- [x] 下单必须冻结规划距离、预计时长、完整计价规则快照及其版本。
- [x] 目的地变更和绕行的正式计费规则留待后续版本。

---

## 6. MVP 自动用券及前端提示（已确认）

### 6.1 已确认结论

MVP 可以默认自动选择本单优惠金额最大的可用券，无需让乘客在完单时再次选择。

但是前端不能只收到一段模糊文本。后端应返回结构化结算结果，前端根据状态和字段展示文案。文本可以由后端给出默认值，但不能成为业务判断的唯一依据。

### 6.2 自动选最优券规则建议

基础规则：

1. 只查询当前乘客、车队、城市、产品线、有效期均匹配的优惠券。
2. 使用优惠前 `finalAmount` 校验门槛并计算每张券的实际优惠。
3. 按实际 `discountAmount` 从大到小选择。
4. 优惠金额相同时，优先使用过期时间更早的券。
5. 仍相同时，以用户券 ID 做稳定排序，保证重试选择结果一致。
6. `payableAmount` 最低为 0，不允许出现负金额。

建议由 `calculate-service` 完成选择与原子锁定；结算编排方传 `couponId=null` 表示“自动选择最优券”。

### 6.3 为什么要先锁券再支付

推荐顺序：

```text
最终计价
  -> 选择并锁定最优券
  -> 固化应付金额
  -> 钱包支付
  -> 支付成功后核销券
```

锁券可以避免同一张券同时被另一笔订单使用。当前实现中，支付明确失败、用户取消及结果待确认期间优惠券均保持 `LOCKED`；支付成功后核销为 `USED`。支付结果未知时只能查询原支付单，不能创建第二笔支付。

### 6.4 建议的优惠结果枚举

```text
APPLIED             已使用优惠券
NO_AVAILABLE        当前没有可用优惠券
NOT_ELIGIBLE        有券但本单不满足使用条件
MANUAL_NO_COUPON    用户明确选择不用券（MVP 可暂不出现）
LOCK_FAILED         原本存在可用券，但锁券发生系统异常
```

`NO_AVAILABLE` 和 `NOT_ELIGIBLE` 是否需要在 MVP 中严格区分，可以继续讨论；后端最好保留差异，前端可以先统一展示。

### 6.5 乘客结算查询接口建议

WebSocket 仍推送通用的 `ORDER_CHANGED`，乘客端收到后重新查询权威 HTTP 数据。可以新增独立接口：

```http
GET /app/api/v1/orders/{orderNo}/settlement
```

建议响应：

```json
{
  "orderNo": "T202607170001",
  "orderStatus": "FINISHED",
  "settlementStatus": "PAID",
  "paymentStatus": "SUCCESS",
  "couponResult": "APPLIED",
  "originalFare": 35.60,
  "discountAmount": 5.00,
  "payableAmount": 30.60,
  "paidAmount": 30.60,
  "coupon": {
    "userCouponId": 8899,
    "couponName": "满30减5元券"
  },
  "displayMessage": "已使用满30减5元券，已优惠5.00元，实付30.60元"
}
```

无券示例：

```json
{
  "orderNo": "T202607170002",
  "orderStatus": "FINISHED",
  "settlementStatus": "PAID",
  "paymentStatus": "SUCCESS",
  "couponResult": "NO_AVAILABLE",
  "originalFare": 18.00,
  "discountAmount": 0.00,
  "payableAmount": 18.00,
  "paidAmount": 18.00,
  "coupon": null,
  "displayMessage": "暂无可用优惠券，本次实付18.00元"
}
```

### 6.6 建议的前端展示

| 场景 | 主文案 | 辅助信息/操作 |
| --- | --- | --- |
| 完单后处理中 | 行程已结束，费用结算中 | 展示加载状态，可自动刷新 |
| 有券且支付成功 | 支付成功 | 已使用 XX 券，优惠 X 元，实付 X 元 |
| 无券且支付成功 | 支付成功 | 暂无可用优惠券，实付 X 元 |
| 不满足券门槛 | 支付成功 | 本单暂无符合条件的优惠券，实付 X 元 |
| 无免密协议 | 待支付 | 引导开通免密或进入主动支付 |
| 自动支付失败 | 支付失败 | 展示原因和“重新支付”入口 |
| 结算长时间处理中 | 费用仍在处理中 | 不展示支付成功，可提示稍后查看 |

可以在两个时间点通知乘客：

1. 订单进入 `FINISHED + PROCESSING`：立即展示“行程已结束，费用结算中”。
2. 结算进入 `PAID` 或 `PAYMENT_FAILED`：再次推送 `ORDER_CHANGED`，前端重新拉取详情和结算结果。

### 6.7 司机端是否需要展示优惠券

初步建议司机端不展示乘客具体优惠券，只展示：

- 行程已结束。
- 优惠前最终车费，或按后续收入规则展示司机可见金额。
- 结算处理中/已结算。

原因是优惠券成本、平台服务费和司机/车队收入仍有独立结算口径，直接给司机展示乘客实付金额可能被误认为司机收入。

### 6.8 本项已确认

- [x] 自动使用优惠金额最大的可用券；同优惠金额时优先较早过期，再按用户券 ID 稳定排序。
- [x] 乘客端以结构化结算字段判断状态，WebSocket 只发送 `ORDER_CHANGED`，不传权威金额。
- [x] 已提供独立结算查询接口；仅 `PAYMENT_REQUIRED` 可主动支付。
- [x] 无免密协议进入 `PAYMENT_REQUIRED`，乘客可选择支付宝或微信主动支付。
- [x] 本期未实现司机金额展示；在车队/运营公司与司机分成规则定版前，不展示“司机净收入”等承诺性金额。

---

## 7. 已实现的完整流程

```text
1. 司机请求完单
   driver-api -> order-service

2. order-service 本地事务
   STARTED -> FINISHED
   写 ORDER_FINISHED
   写 ORDER_SETTLEMENT_REQUESTED Outbox

3. 立即响应司机端
   行程已结束，费用结算中

4. Outbox 投递结算消息
   结算消费者按 orderNo 幂等处理

5. 最终计价
   使用下单冻结的本地 mock 规划距离、预计时长、计价规则快照
   使用服务端首次生成并冻结的 mock 实际计费时长
   calculate-service 返回优惠前 finalAmount

6. 自动用券
   calculate-service 选择并锁定最优券
   返回 discountAmount、payableAmount、couponResult

7. 固化处理中结算快照
   order-service 写 trip_order_settlement
   settlementStatus=CALCULATING

8. 自动支付
   payableAmount=0：跳过钱包支付
   payableAmount>0：wallet-service 按 orderNo 幂等扣款

9A. 支付成功
   核销优惠券（有券时）
   更新结算快照为 PAID
   通知乘客 ORDER_CHANGED

9B. 支付明确失败
   优惠券保持 LOCKED（有券时）
   更新结算快照为 PAYMENT_REQUIRED，等待乘客主动支付
   通知乘客 ORDER_CHANGED

9C. 支付结果未知
   不释放券、不重复建支付单
   查询原支付单并继续推进

10. 乘客端收到通知
    查询订单详情 + 结算详情
    展示是否用券、优惠金额、实付金额和支付状态
```

---

## 8. 服务职责

| 服务 | 职责 | 不应承担的职责 |
| --- | --- | --- |
| `driver-api` | 接收司机完单请求、身份透传、返回行程结束结果 | 决定最终车费、持久编排跨服务支付 |
| `order-service` | 订单状态机、完单事务、可靠发出结算请求、保存结算快照、对外提供权威状态 | 复制计价公式、直接修改券或钱包数据库 |
| `calculate-service` | 最终计价、可用券判断、最优券选择、锁券/核销/释放 | 创建支付单、推进订单状态 |
| `wallet-service` | 免密协议、支付单、幂等扣款、支付结果 | 决定优惠金额、修改订单结算表 |
| `passenger-api` | 聚合订单和结算查询、乘客 WebSocket 通知 | 作为结算任务执行器 |

结算消费者第一阶段可以放在 `order-service` 内，复用订单 Outbox 和结算表；如果后续出现退款、分账、发票、对账等复杂工作流，再评估拆出独立 `settlement-service`。MVP 暂不建议为了一个流程立即新增微服务。

---

## 9. 已覆盖或持续回归的异常场景

实现和测试至少应覆盖：

1. 司机重复点击完单。
2. 两个完单请求并发到达。
3. 订单已经 `FINISHED`，结算消息尚未投递时服务重启。
4. 同一结算 Kafka 消息重复消费。
5. 最终计价服务超时或返回非法金额。
6. 下单时缺少规划距离或计价规则快照。
7. 选券后并发锁券冲突。
8. 没有可用券。
9. 优惠后应付金额为 0。
10. 乘客没有可用免密协议。
11. 钱包支付请求超时但实际已经成功。
12. 钱包明确扣款失败。
13. 支付成功后核券失败。
14. 支付失败、取消或待确认期间优惠券保持 `LOCKED`，支付成功后才核销。
15. 结算已成功但 WebSocket 通知失败。
16. 乘客刷新页面或错过 WebSocket 后仍能通过 HTTP 查到结果。
17. 金额舍入边界，例如 0.005 元、折扣后接近 0 元。
18. 优惠金额相同时能稳定选择同一张券。
19. 订单目的地变化或实际路线严重偏离规划路线。
20. 结算长时间停留在 `CALCULATING` 或 `PAY_CONFIRMING` 时可告警、可人工恢复；持续异常联系运营。

---

## 10. 历史讨论顺序

为了避免接口、表结构和状态反复修改，建议按以下顺序逐项确认：

1. **先确认完单与支付是否解耦**：完单只返回“行程结束、结算中”。
2. **再确认 MVP 计费依据**：下单规划距离 + 服务端实际时长 + 冻结计价规则。
3. **确认自动用券规则**：优惠最大、同额先过期、锁券失败不静默多扣。
4. **确认无免密协议和支付失败后的产品流程**。
5. **确认结算状态机与前端展示字段**。
6. **确认结算编排放在 `order-service`，使用 Outbox + Kafka 可靠触发**。
7. **最后确定表字段、接口契约、补偿任务和专项测试清单**。

---

## 11. 未纳入当前 MVP 的事项

以下事项与完单结算有关，但建议在本次主链路稳定后另行讨论：

- 基于完整 GPS 轨迹的正式计费和异常轨迹申诉。
- 行程中修改目的地、增加途经点、乘客要求绕行。
- 高速费、路桥费、停车费、夜间附加费等人工或外部费用。
- 真实支付宝/微信金融渠道、真实商户签约、验签、公网回调和渠道对账。
- 常规退款、部分退款、组合支付、平台余额、授信和分期。
- 司机金额展示，以及车队/运营公司固定金额或比例分成。
- 退款、部分退款、优惠券退回规则。
- 发票金额和开票主体。
- 司机、车队、平台的最终分账和结算周期。
- 乘客对费用提出异议后的冻结、调账和客服流程。
- 独立 `settlement-service` 的拆分时机。

---

## 12. 讨论结论记录

后续每确认一项，在这里记录最终结论，并同步更新对应 PRD、TECH、API、TEST 文档。

| 编号 | 议题 | 最终结论 | 状态 |
| --- | --- | --- | --- |
| D1 | 完单接口只返回行程结束与结算处理中，不承诺支付成功 | 已确认 | 已落地 |
| D2 | 本地 mock 路线距离、预计时长和服务端冻结的 mock 实际时长；同时冻结计价规则 | 已确认 | 已落地 |
| D3 | 自动选择最大优惠；并列时按较早过期、用户券 ID 排序 | 已确认 | 已落地 |
| D4 | 锁券异常不静默降级为无券支付；保持 `CALCULATING` 并由恢复/运营处理 | 已确认 | 已落地 |
| D5 | 无免密协议进入 `PAYMENT_REQUIRED`，可主动选 `ALIPAY` 或 `WECHAT` | 已确认 | 已落地 |
| D6 | 明确失败不后台定时自动重试；由乘客主动支付，持续异常联系运营 | 已确认 | 已落地 |
| D7 | 保留独立结算查询接口，并已提供主动支付入口 | 已确认 | 已落地 |
| D8 | 结算消费者第一阶段放在 `order-service` 内，通过 Outbox/Kafka 幂等编排 | 已确认 | 已落地 |
