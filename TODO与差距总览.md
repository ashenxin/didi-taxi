# TODO 与差距总览

> 最近核对：2026-09-07（司机行程记录、详情和今日运营看板完成代码与自动化回归；业务 MySQL 待用户手工执行 SQL）
> 核对基准：当前工作区代码、最近提交、P1～P7 执行计划与运行手册、Nacos 配置/服务发现实现，以及 2026-07-17～19 的全链路与自动化回归记录。本文只记录仍未完成、部分完成或明确不开发的事项；已完成功能不再作为 TODO 长篇保留。

## 1. 当前已落地主路径

- 三端统一经 `gateway:18080` 访问，按 `/admin/**`、`/app/**`、`/driver/**` 路由；网关完成 JWT、`aud`、CORS、可信 `X-User-Id` 注入与乘客/司机 WS Upgrade 转发。
- 乘客下单采用“预检 + 创建订单 + Transactional Outbox + Kafka + capacity consumer”异步派单；`Idempotency-Key` 已覆盖创建订单、乘客取消及司机接单/拒单/到达前取消/到达/开始/完单，成功重放不会重复产生权威副作用。
- order 维护权威状态机、订单事件、180 秒总等待取消、30 秒司机确认窗、并发接单互斥和结算快照。
- capacity 已落地 Redis GEO、司机级 Presence/心跳、3km 匹配、迟滞匹配、司机-乘客 30 分钟隔离和消费结果诊断。
- 乘客/司机 WebSocket 单实例链路已落地；HTTP 详情仍是展示权威，WS 用于变更通知与待接单刷新。
- 后台已覆盖登录/菜单/管理员、订单查询、运力、换队审核、计价规则和车队优惠券模板管理，并执行省/市数据域。
- 二期乘客能力已覆盖我的订单、设置/换手机号/注销、钱包免密协议与券查询、车队营销优惠券、券包与登录领券/注销风控、福利签到积分及异常对账补偿，以及完单结算 MVP 本地 mock 闭环。
- 司机端已覆盖成功接单后的服务记录、行程详情和上海自然日运营看板；拒单/确认超时不记行程，接单后取消及改派分别留痕，当前只展示订单金额，不展示司机净收入。
- 乘客账号换号/注销生命周期 P1～P7 代码链路已贯通；新 Lifecycle API 和乘客 H5 已接入，旧 settings 入口默认灰度关闭。
- 除 `xxl-job-admin` 外的 10 个业务服务已接入 Nacos 本地配置、服务注册与 Spring Cloud LoadBalancer；Gateway 已使用 `lb://` 路由三个 BFF，并已完成本地全服务在线与核心业务闭环验收。
- 全仓已接入 JaCoCo 0.8.15；`mvn verify` 为 11 个业务子模块生成 HTML/XML/CSV，并以 1% 行覆盖率作为“采集未失效”的初始门槛。
- 除已有较高覆盖率的 gateway、wallet、calculate 和本轮暂不补测的 xxl-job-admin 外，其余七个服务已新增 21 个风险用例，覆盖后台数据域、乘客设置、地图路线异常、迟滞派单、司机换队、乘客鉴权和订单 Outbox。

## 2. 当前优先 TODO

| 优先级 | 事项 | 当前差距 | 完成口径 |
|---|---|---|---|
| P1 | 真实支付、退款、对账与分成 | 完单结算 MVP 已实现：本地 mock 距离/预计时长/实际时长、冻结计价规则、优惠后支付、结算查询、主动支付和未结清拦截均已贯通；尚无真实渠道回调、退款全流程、对账、发票、司机金额展示和车队/运营公司固定金额或比例分成 | 启动前先确认产品与财务范围；在不改变 MVP 账单快照口径前提下，完成真实支付签名/验签、回调幂等、退款、日对账、异常补偿及独立分成设计 |
| P1 | 后台订单派单诊断聚合 | order/capacity 已有内部诊断接口，admin-api 尚无按 `orderNo` 聚合入口 | 一次查询返回订单状态、事件、Outbox、Kafka 消费结果与卡点建议；FAILED Outbox 可受控重试 |
| P1 | Kafka 坏消息/DLQ | consumer 已记录 `INVALID/MALFORMED` 并 ack，但没有独立死信 topic/表与重放流程 | 原始 payload、topic/partition/offset、错误原因可查询审计；重放需权限和幂等保护 |
| P2 | 订单事件 payload 结构化 | 后台已完成稳定排序、中文事件/状态和操作者展示，但 payload/部分原因仍偏底层 | 拒单、超时、登出释单等原因形成稳定结构化字段，未知新事件保留原始类型便于诊断 |

### 2.1 司机收入与承运侧分成待定项

- 首期只考虑车队/运营公司按单收取固定金额 `FIXED_AMOUNT` 或按比例收取 `PERCENTAGE`。平台服务费仍按乘客优惠后实付金额的 5% 计算，先得到 `carrierIncomeAmount`，再拆分承运侧收入。
- 专项设计需确定计算基数、优惠券成本承担方、固定金额的封顶/保底、比例范围、舍入与尾差，以及公司、车队、司机、城市、产品等规则维度和优先级。
- 每单必须保存分成规则与结果快照，并覆盖生效时间、退款冲正、非负和金额上限校验；异常金额不得进入司机收入统计。
- 规则定版前，司机端只展示“订单总金额”，不得展示司机净收入、可提现金额等承诺性字段。

### 2.2 司机端后续候选功能池

| 优先级 | 候选功能 | 主要前置条件 |
|---|---|---|
| P1 | 司机资料与车辆信息、设置与账号安全、完单结果页、上车验证码 | 复用 capacity/行程详情；账号变更校验进行中订单和资质；验证码需联动乘客端及行程状态机 |
| P2 | 消息中心、服务分/乘客评价、申诉/客服工单、司机钱包/提现、奖励与冲单活动、安全中心/SOS | 分别依赖持久化消息、评价申诉、财务分账与对账、活动风控、位置和线下应急流程 |
| P3 | 接驾导航/ETA、热力图/推荐听单区域、预约单 | 依赖地图 ETA、实时供需聚合，或新增预约订单类型及改期/取消/冲突检测 |

## 3. 已实现但仍需补强测试/运维

| 领域 | 现状 | 仍需补强 |
|---|---|---|
| 网关 | 三端路由、JWT、CORS、身份头、WS 已落地；自动化已覆盖 JWT 验签/跨 audience、生产安全启动、伪造身份头清理、缺失 token、公开路径/OPTIONS、Request-Id 与下游 CORS 头清理；JaCoCo 行覆盖率 73.88% | 真实 WS Upgrade/小票握手仍需端到端回归；生产限流、熔断、指标告警待补 |
| 后台 | 主要 BFF 和数据域已落地；自动化已补城市/省级管理员查询范围、跨区域拒绝和写入区域覆盖；JaCoCo 行覆盖率 8.38% | 继续补角色权限矩阵、换队审核写操作、越权修改和下游异常映射 |
| 钱包/优惠券 | 完单结算 MVP 已完成本地 mock 编排；金额在冻结计价规则下先减优惠后支付，零元单单独结清；已提供结算查询、主动支付和未结清拦截 | 真实支付/退款/对账前必须重做高风险回归与财务评审；司机金额展示与车队/运营公司固定金额或比例分成待专项实现 |
| 福利签到 | XXL-Job 对账已落地：当月 Bitmap 只补缺失位，历史月按 MySQL 精确重建；MySQL 签到/流水/账户差异幂等写入 `benefit_reconciliation_issue`，不自动改积分 | 运行环境持续观察 XXL-Job 日志与 HIGH 问题；异常仍由运营/研发人工核实处理，暂不增加内部触发接口和自动改分 |
| 地图 | 高德地理编码和驾车规划 Demo 已接入；已覆盖路线成功解析、业务失败和 Key 缺失；JaCoCo 行覆盖率 32.89% | 当前不提供真实“司机位置到上车点”接驾 ETA；外部限流、超时和部署环境降级仍需验证 |
| 测试度量 | JaCoCo 已接入并生成模块级报告；当前行覆盖率为 gateway 73.88%、wallet 55.28%、order 48.56%、calculate 48.22%、passenger-api 41.13%、driver-api 35.41%、map 32.89%、capacity 29.99%、passenger 18.90%、admin-api 8.38% | 1% 仅防止覆盖率采集完全失效；下一轮优先继续补 admin-api、passenger 的高风险业务分支，不得为追求数字补无断言测试 |
| 文档测试 | 已拆为各专项 `*_TEST.md` | 测试文档是验收口径，不代表已自动执行；后续新增功能必须同步专项 TEST |
| Nacos 与服务发现 | 10 个业务服务已完成本地配置导入、注册发现、Feign/RestClient/Gateway 负载均衡和边界测试；本地全服务在线、跨服务调用与核心完单闭环已验收 | 生产部署前继续验证 Nacos 不可用快速失败、实例摘除、告警和恢复，不再阻塞本地架构验收 |
| 乘客账号生命周期 | P1～P7 代码、Lifecycle API、受限会话、参与者、Outbox/Kafka、Recovery 和 H5 已贯通；生产验收已完成（本轮范围不含灰度观察与双 MySQL 并发专项） | 后续按生产运行监控和 Legacy Adapter 下线计划推进 |

## 4. 明确暂缓或本阶段不开发

- 乘客/司机 WebSocket 多实例 Sticky、Redis Pub/Sub 或 MQ 广播：当前按单实例验收。
- 真实接驾 ETA/地图矩阵：暂缓，不阻塞下单派单闭环。
- Redis 订单池与 DB 的强制对账任务：Redis 仍是派生索引，按实际故障收益再建设。
- OAuth2 Resource Server 标准栈：当前 JJWT + 按端 `aud` 已满足本地项目需求。
- 多券叠加、平台券、复杂周期营销规则：不在当前车队优惠券范围。
- 取消费、附加费、小费，以及司机/车队内部收费与分账：另立财务/结算专题。

## 5. 文档与测试入口

### 核心链路

- `第一期MVP_乘客派单司机闭环_{PRD,TECH,API,TEST}.md`
- `订单与派单_两段式Outbox与Kafka_技术方案.md`
- `订单与派单_订单服务幂等与并发方案说明.md`
- `订单与派单_TEST.md`
- `乘客司机端_Redis与听单下线策略.md`

### 基础设施与鉴权

- `网关服务_{设计,技术,TEST}.md`
- `乘客端_登录_{PRD,TECH,API,TEST}.md`
- `司机端_登录注册_{PRD,TECH,API,TEST}.md`
- `docs/runbooks/capacity-service-Nacos本地配置运行手册.md`

### 乘客账号生命周期

- `docs/plans/乘客账号生命周期P1-P7执行计划索引.md`
- `docs/runbooks/乘客账号生命周期P7灰度切换与前向恢复手册.md`

### 后台

- `后台管理系统_权限清单与鉴权设计.md`
- `后台管理系统_权限与接口文档.md`
- `后台管理系统_权限_TEST.md`
- `后台管理系统_{订单管理,运力配置,计价管理}_{PRD,TECH,API,TEST}.md`

### 二期乘客功能

- 统一索引见 `二期功能/README.md`。
- 每个正式功能以同名 PRD/TECH/API/TEST 为一组；福利签到另含 SQL 和异常补偿 TECH，车队营销优惠券另含 SQL。

### 完单结算 MVP

- `docs/superpowers/specs/2026-07-17-完单结算_DESIGN.md`
- `docs/superpowers/plans/2026-07-17-完单结算_PLAN.md`
- `docs/api/完单结算_API.md`
- `docs/testing/完单结算_TEST.md`
- `乘客司机端_完单结算方案讨论.md`（历史讨论与决策追溯）

## 6. 修订记录

| 日期 | 说明 |
|---|---|
| 2026-04-25 | 合并原后台 TODO 与全仓未实现清单。 |
| 2026-06-13 | 更新 Outbox/Kafka、Presence、WS 和诊断能力。 |
| 2026-07-07 | 定版车队营销优惠券核心金额与权限口径。 |
| 2026-07-16 | 按当前代码重新核对；移除已经完成的优惠券后台/表结构等伪 TODO，补充券包风控、福利签到、真实支付、司机近期功能与测试入口。 |
| 2026-07-17 | 记录全链路实测；确认健康检查、后台分页、事件中文映射和重复路由修复生效；完单结算 MVP 已落地，后续聚焦真实支付/退款/对账与分成。 |
| 2026-07-18 | 同步网关安全过滤器、司机订单 HTTP 身份隔离、capacity 派单/改派/事件幂等及其余七服务风险测试结果；更新 JaCoCo 模块基线，统一完单结算文档命名；完成下单与接单成功重放的 BFF 外部调用短路，并为乘客取消及司机接单/拒单/到达前取消/到达/开始/完单补齐请求级幂等。 |
| 2026-07-19 | 落地福利签到异常对账补偿：XXL-Job 收敛 Redis Bitmap，单表留痕 MySQL 差异并供人工处理，不自动改积分、不增加内部 HTTP 触发接口。 |
| 2026-07-31 | 根据最近代码与提交重新核对：补充乘客生命周期 P1～P7 和全业务服务 Nacos 现状；将当前最高优先级收敛为“环境验收→生命周期故障演练与灰度”，真实支付和司机新功能下调为后续产品开发项。 |
| 2026-08-01 | 完成当前架构本地可运行验收：10 个业务服务在 Nacos 确认在线，并实测乘客下单、异步派单、司机接单、行程推进、完单结算、主动 Mock 支付、Wallet 结果通知及解除新下单限制；移除对应 P0。验收订单号：`202608011036290878498`。 |
| 2026-08-03 | 完成乘客账号生命周期生产验收：注销受理与进度轮询、幂等、Passenger/Kafka 与 Calculate 故障恢复、最终一致性及注销后新账号登录/历史订单隔离均通过；按已确认范围移除对应 TODO。 |
| 2026-09-06 | 用户再次确认生命周期已验收；同步 README、协作说明、阶段索引与运行手册，保留原验收范围。核对优惠券已有实现并修正前端过时文档；启动司机行程记录、详情和今日运营看板的方案讨论，尚未标记功能完成。 |
| 2026-09-07 | 完成司机行程记录、详情和今日运营看板：新增逐次接单服务记录、司机隔离查询、上海自然日统计、司机 BFF 与 H5 页面；补齐 PRD/API/TECH/TEST/SQL、带注释建表和历史回填脚本。Order 122 项、driver-api 53 项及前端契约检查/构建通过。 |
