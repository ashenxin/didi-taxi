# TODO 与差距总览

> 最近核对：2026-07-18
> 核对基准：当前工作区代码、2026-07-17 全链路启动/真实浏览器/接口回归，以及 2026-07-18 自动化回归与 JaCoCo 基线。本文只记录仍未完成、部分完成或明确不开发的事项；已完成功能不再作为 TODO 长篇保留。

## 1. 当前已落地主路径

- 三端统一经 `gateway:18080` 访问，按 `/admin/**`、`/app/**`、`/driver/**` 路由；网关完成 JWT、`aud`、CORS、可信 `X-User-Id` 注入与乘客/司机 WS Upgrade 转发。
- 乘客下单采用“预检 + 创建订单 + Transactional Outbox + Kafka + capacity consumer”异步派单；`Idempotency-Key` 已覆盖创建订单，成功重放会在 passenger-api 调用地图、运力和计价前短路并返回原冻结快照。
- order 维护权威状态机、订单事件、180 秒总等待取消、30 秒司机确认窗、并发接单互斥和结算快照。
- capacity 已落地 Redis GEO、司机级 Presence/心跳、3km 匹配、迟滞匹配、司机-乘客 30 分钟隔离和消费结果诊断。
- 乘客/司机 WebSocket 单实例链路已落地；HTTP 详情仍是展示权威，WS 用于变更通知与待接单刷新。
- 后台已覆盖登录/菜单/管理员、订单查询、运力、换队审核、计价规则和车队优惠券模板管理，并执行省/市数据域。
- 二期乘客能力已覆盖我的订单、设置/换手机号/注销、钱包免密协议与券查询、车队营销优惠券、券包与登录领券/注销风控、福利签到积分，以及完单结算 MVP 本地 mock 闭环。
- 全仓已接入 JaCoCo 0.8.15；`mvn verify` 为 11 个业务子模块生成 HTML/XML/CSV，并以 1% 行覆盖率作为“采集未失效”的初始门槛。
- 除已有较高覆盖率的 gateway、wallet、calculate 和本轮暂不补测的 xxl-job-admin 外，其余七个服务已新增 21 个风险用例，覆盖后台数据域、乘客设置、地图路线异常、迟滞派单、司机换队、乘客鉴权和订单 Outbox。

## 2. 当前优先 TODO

| 优先级 | 事项 | 当前差距 | 完成口径 |
|---|---|---|---|
| P0 | 真实支付、退款、对账与分成 | 完单结算 MVP 已实现：本地 mock 距离/预计时长/实际时长、冻结计价规则、优惠后支付、结算查询、主动支付和未结清拦截均已贯通；尚无真实渠道回调、退款全流程、对账、发票、司机金额展示和车队/运营公司固定金额或比例分成 | 在不改变 MVP 账单快照口径前提下，完成真实支付签名/验签、回调幂等、退款、日对账、异常补偿、财务评审及独立分成设计 |
| P0 | 司机端近期功能 | 行程记录、今日运营看板、行程详情等尚未开发 | 以 `二期功能/司机端_下周开发_TODO.md` 的范围与验收口径推进 |
| P1 | 后台订单派单诊断聚合 | order/capacity 已有内部诊断接口，admin-api 尚无按 `orderNo` 聚合入口 | 一次查询返回订单状态、事件、Outbox、Kafka 消费结果与卡点建议；FAILED Outbox 可受控重试 |
| P1 | Kafka 坏消息/DLQ | consumer 已记录 `INVALID/MALFORMED` 并 ack，但没有独立死信 topic/表与重放流程 | 原始 payload、topic/partition/offset、错误原因可查询审计；重放需权限和幂等保护 |
| P1 | 关键写接口幂等扩展 | 目前请求级幂等主要覆盖创建订单；取消、接单、拒单、到达前取消、完单仍主要依赖状态 CAS | 同 key 同请求返回同一结果；同 key 不同请求 409；不重复写事件、Outbox、支付或结算 |
| P1 | 福利签到异常补偿 | MySQL 为权威、Redis Bitmap 为辅助；目前异常只记录日志，没有自动对账补偿 | 可按 MySQL 重建 Bitmap，并能发现/修复签到记录、积分流水和账户余额差异 |
| P2 | 订单事件 payload 结构化 | 后台已完成稳定排序、中文事件/状态和操作者展示，但 payload/部分原因仍偏底层 | 拒单、超时、登出释单等原因形成稳定结构化字段，未知新事件保留原始类型便于诊断 |

## 3. 已实现但仍需补强测试/运维

| 领域 | 现状 | 仍需补强 |
|---|---|---|
| 网关 | 三端路由、JWT、CORS、身份头、WS 已落地；自动化已覆盖 JWT 验签/跨 audience、生产安全启动、伪造身份头清理、缺失 token、公开路径/OPTIONS、Request-Id 与下游 CORS 头清理；JaCoCo 行覆盖率 73.88% | 真实 WS Upgrade/小票握手仍需端到端回归；生产限流、熔断、指标告警待补 |
| 后台 | 主要 BFF 和数据域已落地；自动化已补城市/省级管理员查询范围、跨区域拒绝和写入区域覆盖；JaCoCo 行覆盖率 8.38% | 继续补角色权限矩阵、换队审核写操作、越权修改和下游异常映射 |
| 钱包/优惠券 | 完单结算 MVP 已完成本地 mock 编排；金额在冻结计价规则下先减优惠后支付，零元单单独结清；已提供结算查询、主动支付和未结清拦截 | 真实支付/退款/对账前必须重做高风险回归与财务评审；司机金额展示与车队/运营公司固定金额或比例分成待专项实现 |
| 地图 | 高德地理编码和驾车规划 Demo 已接入；已覆盖路线成功解析、业务失败和 Key 缺失；JaCoCo 行覆盖率 32.89% | 当前不提供真实“司机位置到上车点”接驾 ETA；外部限流、超时和部署环境降级仍需验证 |
| 测试度量 | JaCoCo 已接入并生成模块级报告；当前行覆盖率为 gateway 73.88%、wallet 55.28%、order 48.56%、calculate 48.22%、passenger-api 41.13%、driver-api 35.41%、map 32.89%、capacity 29.99%、passenger 18.90%、admin-api 8.38% | 1% 仅防止覆盖率采集完全失效；下一轮优先继续补 admin-api、passenger 的高风险业务分支，不得为追求数字补无断言测试 |
| 文档测试 | 已拆为各专项 `*_TEST.md` | 测试文档是验收口径，不代表已自动执行；后续新增功能必须同步专项 TEST |

## 4. 明确暂缓或本阶段不开发

- 乘客/司机 WebSocket 多实例 Sticky、Redis Pub/Sub 或 MQ 广播：当前按单实例验收。
- 真实接驾 ETA/地图矩阵：暂缓，不阻塞下单派单闭环。
- Redis 订单池与 DB 的强制对账任务：Redis 仍是派生索引，按实际故障收益再建设。
- OAuth2 Resource Server 标准栈：当前 JJWT + 按端 `aud` 已满足本地项目需求。
- 多券叠加、平台券、复杂周期营销规则：不在当前车队优惠券范围。
- 取消费、附加费、小费，以及司机/车队内部收费与分账：另立财务/结算专题。

## 5. 文档与测试入口

### 核心链路

- `第一期MVP_乘客派单司机闭环_{PRD,TECH,API,TEST}.md`
- `订单与派单_两段式Outbox与Kafka_技术方案.md`
- `订单与派单_订单服务幂等与并发方案说明.md`
- `订单与派单_TEST.md`
- `乘客司机端_Redis与听单下线策略.md`

### 基础设施与鉴权

- `网关服务_{设计,技术,TEST}.md`
- `乘客端_登录_{PRD,TECH,API,TEST}.md`
- `司机端_登录注册_{PRD,TECH,API,TEST}.md`

### 后台

- `后台管理系统_权限清单与鉴权设计.md`
- `后台管理系统_权限与接口文档.md`
- `后台管理系统_权限_TEST.md`
- `后台管理系统_{订单管理,运力配置,计价管理}_{PRD,TECH,API,TEST}.md`

### 二期乘客功能

- 统一索引见 `二期功能/README.md`。
- 每个正式功能以同名 PRD/TECH/API/TEST 为一组；福利签到另含 SQL，车队营销优惠券另含 SQL。

### 完单结算 MVP

- `docs/superpowers/specs/2026-07-17-完单结算_DESIGN.md`
- `docs/superpowers/plans/2026-07-17-完单结算_PLAN.md`
- `docs/api/完单结算_API.md`
- `docs/testing/完单结算_TEST.md`
- `乘客司机端_完单结算方案讨论.md`（历史讨论与决策追溯）

## 6. 修订记录

| 日期 | 说明 |
|---|---|
| 2026-04-25 | 合并原后台 TODO 与全仓未实现清单。 |
| 2026-06-13 | 更新 Outbox/Kafka、Presence、WS 和诊断能力。 |
| 2026-07-07 | 定版车队营销优惠券核心金额与权限口径。 |
| 2026-07-16 | 按当前代码重新核对；移除已经完成的优惠券后台/表结构等伪 TODO，补充券包风控、福利签到、真实支付、司机近期功能与测试入口。 |
| 2026-07-17 | 记录全链路实测；确认健康检查、后台分页、事件中文映射和重复路由修复生效；完单结算 MVP 已落地，后续聚焦真实支付/退款/对账与分成。 |
| 2026-07-18 | 同步网关安全过滤器、司机订单 HTTP 身份隔离、capacity 派单/改派/事件幂等及其余七服务风险测试结果；更新 JaCoCo 模块基线，统一完单结算文档命名，并完成下单成功重放的 BFF 外部调用短路。 |
