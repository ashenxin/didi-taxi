# 本地开发：未启动 Zipkin 时使用，避免向 9411 上报失败刷 WARN。
# 启用方式（任选其一）：
#   - IntelliJ Run Configuration → Active profiles: local
#   - VM options: -Dspring.profiles.active=local
#   - 环境变量: SPRING_PROFILES_ACTIVE=local

management:
  tracing:
    export:
      zipkin:
        enabled: false
