server:
  port: 8093

spring:
  application:
    name: order-service
  # 需要注册中心时取消注释并启动 Nacos
  #  cloud:
  #    nacos:
  #      discovery:
  #        server-addr: 127.0.0.1:8848
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/order?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      validation-query: SELECT 1
      test-while-idle: true
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      properties:
        enable.idempotence: true

logging:
  config: classpath:logback-spring.xml
  file:
    path: /Users/sx/Documents/workSpace/didi-taxi/order/logs/order-service

management:
  tracing:
    sampling:
      probability: 1.0
    export:
      zipkin:
        enabled: false
  zipkin:
    tracing:
      endpoint: http://127.0.0.1:9411/api/v2/spans

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
  mapper-locations: classpath*:mapper/**/*.xml

# 派单相关定时：XXL 与下方 Spring 任务勿重复跑同一逻辑（控制台周期可与这里对齐或二选一）
order:
  dispatch:
    wait-timeout-seconds: 180
    timeout-scan-interval-ms: 30000
    # 待司机确认(7) 过期扫描：略小于确认窗，保证打回 ASSIGNED 的滞后可控（默认窗 30s + 本项 5s ≈ 最坏多等 5s）
    offer-timeout-scan-interval-ms: 5000

# XXL-JOB 执行器：需独立部署 xxl-job-admin，版本需与根 pom 中 xxl.job.version 对齐
xxl:
  job:
    enabled: true
    access-token: ${XXL_JOB_ACCESS_TOKEN:default_token}
    admin:
      addresses: http://127.0.0.1:8080/xxl-job-admin
    executor:
      app name: order-executor
      address: ${XXL_JOB_EXECUTOR_ADDRESS:http://127.0.0.1:9998}
      port: 9998
      log-path: ./logs/xxl-job
      log-retention-days: 7

---
spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    com.sx.order.dao: DEBUG

---
spring:
  config:
    activate:
      on-profile: local

logging:
  level:
    com.sx.order.dao: DEBUG
