# 地图服务 TEST

本文覆盖当前 `map` 服务和 passenger-api 下单时的地图调用。当前已经接入高德地理编码与驾车规划 Demo，但不提供权威“司机位置到上车点”接驾 ETA。

## 0. 环境准备

- 启动 map、passenger-api；经端侧测试时同时启动 gateway。
- 配置有效高德 Key；网络不可用时只执行参数和降级类用例。
- 准备杭州地址、明确经纬度、跨城市地址、无效地址和边界坐标。
- 记录外部请求耗时和高德返回码，但日志不得打印 Key。

## 1. 地理编码

### T-MAP-01 正常地址

调用当前 `amap-geocode` 接口，输入完整省市区和道路地址。

预期：返回合法经纬度、城市信息；纬度在 [-90,90]、经度在 [-180,180]；passenger-api 可直接使用结果。

### T-MAP-02 模糊和多结果地址

输入“火车站”等模糊地址。

预期：选择规则稳定或明确返回候选不足错误；不得随机返回不同城市结果而不带城市信息。

### T-MAP-03 无结果地址

输入不存在的地址和空白地址。

预期：空白参数 400；外部无结果转换为明确业务失败，不伪造 0,0 坐标。

### T-MAP-04 Key 无效、限流和超时

分别使用错误 Key、模拟外部限流和连接超时。

预期：返回可识别的 4xx/5xx/业务错误；日志含外部错误码和 traceId，不含 Key；不得无限重试阻塞 BFF。

## 2. 驾车规划

### T-MAP-05 正常路线

调用 `/api/v1/map/demo/amap-driving`，传合法 origin/dest。

预期：distanceMeters、durationSeconds 为非负合理值；路径结构可反序列化；起终点对应请求。

### T-MAP-06 同点和短距离

传相同或极近坐标。

预期：按地图返回稳定处理；不得出现负距离、NaN 或溢出。

### T-MAP-07 非法坐标

测试纬度 91、经度 181、null、字符串和经纬度颠倒的典型值。

预期：参数层拒绝明显非法值；不向高德发送无意义请求。

### T-MAP-08 跨城市和无可驾车路线

使用跨城市长路线、海上点或不可达点。

预期：长路线可返回合理结果；无路线时明确失败，不返回假的直线距离当驾车结果。

## 3. passenger-api 下单集成

### T-MAP-09 地址转坐标后下单

请求只提供地址、不提供坐标。

预期：BFF 先 geocode，再 driving，再 estimate/create；任一步失败不创建订单和 Outbox。

### T-MAP-10 已有坐标跳过 geocode

请求同时提供合法起终点经纬度。

预期：跳过地理编码，直接规划路线；适用于本地无地址服务时联调。

### T-MAP-11 地图成功但后续失败

让 calculate 或 order 不可用。

预期：地图成功不代表下单成功；响应明确下游失败，不能返回不存在的 orderNo。

### T-MAP-12 Idempotency-Key 重试

因客户端超时重试相同下单。

预期：最终订单幂等由 order 保证；地图可能被重复调用但不得造成重复订单。性能优化缓存不改变业务结果。

## 4. ETA 边界

### T-MAP-13 `etaSeconds` 展示

检查乘客/司机订单详情。

预期：当前 `etaSeconds` 不得被文档或前端解释成真实“司机当前位置到上车点”ETA；为空时使用兜底文案，不展示误导性倒计时。

## 5. 非功能

### T-MAP-14 外部调用耗时

连续执行正常、超时和限流请求。

预期：连接/读取超时受配置约束；线程不持续堆积；日志可按 traceId 定位慢调用。

### T-MAP-15 敏感配置

检查响应、异常和日志。

预期：高德 Key、完整内部 URL 凭据不得返回客户端。

## 6. 自动化回归

```bash
mvn -pl map test
mvn -pl passenger-api test
```

外部高德测试受网络和配额影响，应单独标记环境失败；参数校验和 BFF 编排测试不能因此跳过。
