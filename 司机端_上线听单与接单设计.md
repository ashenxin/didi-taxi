# 司机端：上线、听单与接单（目标设计）

本文档固化 **司机端实时通道 + 派单确认 + 会话与调度** 的**已拍板结论**，与《`乘客司机端_最小闭环接口调用文档.md`》《`司机端_WebSocket与实时协议入门.md`》《`乘客司机端_Redis与听单下线策略.md`》交叉引用。  
**当前仓库代码可能尚未实现**；实现以 PR 为准，本文用于需求对齐与评审。

---

## 0. 文档定位（与“最小闭环 / 一期 MVP”关系）

- **定位**：本文件是「司机端听单/接单子域」的**规则与语义细化**（状态机、确认窗口、presence/心跳、并发与幂等口径），用于落地实现与评审。
- **与《乘客司机端_最小闭环接口调用文档.md》**：**被引用关系**。最小闭环文档负责端到端总览与联调入口，本文件负责司机端细节规则。
- **与《第一期MVP_乘客派单司机闭环_API/PRD/TECH/TEST.md》**：**从属关系**。一期 MVP 四份文档负责“项目级拆分与验收口径”，本文件作为 TECH 细化补充被引用；避免在多处重复维护同一套规则。
- **接口契约归属**：接口字段与路径以 API 文档为准（本文件尽量只写“语义/规则”，减少字段重复）。

---

## 1. 文档范围

- **上线 / 听单**：`monitor_status`、presence、WebSocket、心跳。
- **Redis 司机池（规划）**：听单中司机位置索引（GEO 等）、**TTL + 心跳**、**下线/登出须从 Redis 司机池删除**；**与上车点距离 ≤3km** 才可参与匹配；**端到端流程图**见《`乘客司机端_Redis与听单下线策略.md`》**§8**。
- **接单**：派单确认窗口、`POST .../accept` 复用、状态机、调度扫描。
- **会话**：同一手机号全局唯一有效 JWT、`token_version` + Redis、登出强制作废。
- **不包含**：具体类名、网关 XML 片段（见网关技术文档与实现 PR）。

---

## 2. 订单状态机（目标）

### 2.1 主路径

```text
CREATED → ASSIGNED → PENDING_DRIVER_CONFIRM → ACCEPTED → ARRIVED → STARTED → FINISHED
```

- **`ASSIGNED`**：已写入 **`driver_id`**，乘客端展示 **「已派单」**（**不**再细分「待司机确认」文案）。
- **`PENDING_DRIVER_CONFIRM`**：已进入 **派单确认窗口**，服务端写入 **`offerExpiresAt`**、**`offer_round`**、**`last_offer_at`** 等；司机需在窗口内 **`accept`**。
- **确认窗口时长**：**10s**，在**配置文件写死**（本阶段不按城市/产品线区分；**不考虑多时区**，时间以服务端为准）。

### 2.2 超时（未在 `offerExpiresAt` 前 `accept`）

- **先回到 `ASSIGNED`**（**不**直接清空业务语义上的「已派单」）。
- 订单上的 **`driver_id` 仍指向本轮超时那位司机**；后续 **改派 / 下一轮 offer** 由 **调度异步任务** 推进（更新 `driver_id`、`offer_round` 等后再进入 `PENDING_DRIVER_CONFIRM`）。
- **超时、改派**均写入 **`order_event`** 流水，便于审计与排障。

### 2.3 乘客取消

- 在 **`PENDING_DRIVER_CONFIRM`（及前置状态）** 允许乘客取消 → **`CANCELLED`**（规则与现有取消域一致）。

### 2.4 司机侧 `accept`

- **复用** **`POST /driver/api/v1/orders/{orderNo}/accept`**（或 order-service 对内等价接口）；合法状态需包含 **`PENDING_DRIVER_CONFIRM`**（及迁移期兼容策略，由实现定）。

### 2.5 多笔待确认与「服务中」互斥（order-service 已落地）

- **列表**：**`ASSIGNED`**、**`PENDING_DRIVER_CONFIRM`** 均可出现在司机轮询列表（`GET .../orders/assigned`）；司机可刷新拉多笔待确认单。
- **接单**：司机 **`accept` 其中一单** 进入 **`ACCEPTED`** 后，**同司机**其余 **`ASSIGNED` / `PENDING_DRIVER_CONFIRM`** 订单由系统 **自动取消**（`cancel_by=系统`，原因「司机已接其他订单」），保证不会双接单。
- **再派单**：若司机已有 **`ACCEPTED`～`STARTED`**（已接单～行程中）订单，**`assign` 拒绝**（业务码 **409**「司机正在服务中，无法指派新订单」）。**`FINISHED` / `CANCELLED`** 后不再占用，可再派。

**联调前端（`didi-driver-h5`）**  
仓库路径示例：**`didi-taxi-front/didi-driver-h5`**。经网关访问 **`driver-api`**，主要页面调用 **`GET .../orders/assigned`**（刷新指派单）、**`POST .../orders/{orderNo}/accept`** 等；实现上对 **多笔待确认**、**行程跟单中无新指派**、**409** Toast 等做了展示与兜底，与上列规则一致。字段级与文件级说明见《`乘客司机端_最小闭环接口调用文档.md`》**§4.2**「司机端 H5」。

**与《`乘客司机端_Redis与听单下线策略.md`》§7 对齐**：业务语义均为 **「仅一单 accept 生效，其余待确认单作废」**；「作废」与「系统取消」的具体落库形态以 order 实现为准。

---

## 3. 调度与幂等

- **异步扫描周期**：**每 5s** 扫描待处理订单（`ASSIGNED` / `PENDING_DRIVER_CONFIRM` 等，具体 WHERE 条件由实现定）。
- **迟滞匹配（`CREATED` → 指派）**：与上项 **订单侧** 扫描不同，推荐 **司机入池事件（主）+ 定时任务（兜底）** 触发对 **待派单订单** 的「最近司机 + 距离 ≤3km + DB `assign`」，详见《`乘客司机端_Redis与听单下线策略.md`》**§6**；避免与「入池」重复劳动时，定时周期可略拉长或由实现统一。
- **幂等**：同一订单、同一事件不得重复推进；使用 **CAS + `order_event`** 或分布式锁，避免多实例重复执行。
- **司机离线**：**capacity / presence** 尽快落库；**订单状态推进**以 **调度扫描** 为主（与「断线即更新司机维度」配合）。

---

## 4. 会话与 JWT（同一手机号全局一个有效 Token）

- **策略**：**`token_version` + Redis**（实现：JWT claim **`tv`**，Redis **`driver:tv:{driverId}`**；**`driver-api`** 校验 **`tv` == Redis**，网关验签与 **`aud`**）。
- **语义**：同一手机号 **新登录** 使 **旧 JWT 全局失效**；旧终端 HTTP **401**、WebSocket **断开或握手失败**。
- **登出**：**`POST /driver/api/v1/auth/logout`**（**`driver-api`**，须 Bearer）对 **`driver:tv:{driverId}`** **INCR**，强制作废当前及此前 JWT；协议见《`司机端_登录注册_API.md`》§7。**待确认 offer 不再发给该司机**（订单侧按规则剔除或等待调度处理）为业务侧目标，与 HTTP 登出分期协同。
- **Redis**：与《`司机端_登录注册_TECH.md`》一致；**实现键名** **`driver:tv:{driverId}`**。

---

## 5. WebSocket 与 presence

### 5.1 协议选型

- **实时层仅使用 WebSocket**（**不**并行引入 SSE 作为同类能力）。
- **乘客端**后续也改为 **WebSocket**（另文/另迭代；与司机可共用网关路径规范或分路径）。

### 5.2 部署形态（最小工作量）

- **优先**：在 **`driver-api` 进程内** 提供 WebSocket Endpoint（**最小拆分**、单仓库联调快）。
- **网关**：**必须**支持 **WebSocket 升级** 与转发；**后续多实例**时，通过 **Sticky** 或 **Redis Pub/Sub（或消息队列）** 解决跨节点推送，实现阶段选型。

### 5.3 握手鉴权

- **握手携带 `Authorization: Bearer <JWT>`**（与 HTTP 一致）；**未通过审核不允许建立 WS**（握手直接拒绝），业务规则最简单；HTTP 仍可用于登录后查看原因/补件。

### 5.4 心跳与离线

- **间隔 15s**；**连续 3 次**未收到应用层心跳/应答 → **判离线**，**写数据库**（如 `monitor_status` / 在线标记），**不能接单**。
- **与派单 10s 倒计时独立**，互不替代。
- **单终端**：**新连接踢旧连接**；与 **全局唯一 JWT** 一致。

### 5.5 消息 envelope

- **统一格式**（`v` / `type` / `payload` 等），**HTTP 订单字段与 WS 推送字段同一套命名**。
- **`messageId`**：每条可重放消息携带；服务端 **Redis 去重**，**保留 8 小时**（为后续 **防疲劳 / 重复点击** 等留扩展空间）。

---

## 6. 订单表与流水（建议字段）

| 字段 / 流水 | 说明 |
|-------------|------|
| **`offer_expires_at`** | 当前确认窗口截止时间（服务端写入）。 |
| **`offer_round`** | 第几轮 offer（同司机重复派单时递增）。 |
| **`last_offer_at`** | 最近一次发起 offer 的时间。 |
| **`order_event`** | **超时、改派、确认、取消** 等均记流水，类型与 payload 实现时枚举。 |

---

## 7. 观测与运维

- **本期不做**专项观测大盘（连接数、心跳失败率等）；后续再补。

---

## 8. 关联文档

| 文档 | 内容 |
|------|------|
| 《`乘客司机端_最小闭环接口调用文档.md`》 | 总览、乘客端、网关与 JWT；**§4.2** 含司机端 H5（**`didi-driver-h5`**）接口与页面行为，与本文 **§2.5** 对齐 |
| 《`乘客司机端_Redis与听单下线策略.md`》 | Redis 司机池、3km、TTL/心跳、下线删池、迟滞匹配、多单一司机、**listAssigned 策略 A**、**§8 端到端流程图** |
| 《`司机端_登录注册_API.md` / `司机端_登录注册_PRD.md` / `司机端_登录注册_TECH.md`》 | 司机端登录注册接口/产品/技术口径（含 `tv/token_version`、登出、Redis 风控） |
| 《`司机端_WebSocket与实时协议入门.md`》 | 名词入门 |
| 《`网关服务_技术.md`》 | 网关能力与 WebSocket 注意点 |

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-13 | **§1 / §3 / §8**：Redis 司机池与迟滞匹配分工；**§2.5** 与《司机池…》§7 互引；关联表增加《司机池…》。**§2.5** 多笔待确认、`accept` 后系统取消其余单；**`ACCEPTED`～`STARTED`** 期间禁止再 `assign`（409）；联调 **`didi-driver-h5`** 与《`乘客司机端_最小闭环接口调用文档.md`》**§4.2** / **§8** 对齐。 |
| 2026-04-13 | **§4**：与实现对齐 — JWT **`tv`**、Redis **`driver:tv:*`**、登出 **`POST .../auth/logout`** 指向《`司机端_登录注册_API.md`》§7。 |
| 2026-04-11 | 初稿：状态机、5s 调度、`token_version`、WS 内嵌 driver-api、`messageId` 8h、`order_event`、观测暂不做 |

*修订时请同步更新「关联文档」中的状态机与字段表。*
